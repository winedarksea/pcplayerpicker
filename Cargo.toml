[workspace]
resolver = "2"
members = [
    "app_core",
    "client_app",
    "server_cloudflare",
]

[profile.release]
lto = true
opt-level = 'z'
strip = true
codegen-units = 1

[profile.client-release]
inherits = "release"
opt-level = 3
